package com.sachintests;

import java.io.*;

// Default Constructor with no arguments ot available
public class SomeRecord {
 /*   public SomeRecord(String title){
        set_title(title);
    }
    public String get_title() {
        return _title;
    }
    public void set_title(String _title) {
        this._title = _title;
    }
    private String _title;
    public void WriteSomeRecord(ObjectOutputStream out) throws IOException {
        // Serialize object - Write
        out.write(get_title().getBytes());
    }
    public void ReadSomeRecord(ObjectInputStream in) throws IOException, ClassNotFoundException {
        // Deserialize Object - Read
        byte[] buffer = new byte[1024];
        in.readFully(buffer);
        set_title(new String(buffer));
    }*/
}