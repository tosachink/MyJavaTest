package com.sachintests;

import java.io.File;

public class CommonFunctions {
/*
    // Array Index out of bound
    public void performArrayIteration(int[] array) {
        for(int index = 0; index <= array.length; index++) {
            array[index] = index * 2;
        }
    }
    // In correct API usage results in files with empty names
    public File performIncorrectAPIUsage(String filename){
        if(null != filename && filename.length() >= 0 ){
            return new File(filename);
        }
        return null;
    }
    public boolean areEqual(int[] array1, int[] array2){
        return array1.equals(array2);
    }
 */
}